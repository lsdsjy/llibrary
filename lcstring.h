#ifndef LCSTRING_H_INCLUDED
#define LCSTRING_H_INCLUDED

#include "lstdlib.h"

sizet lstrlen(const char*);
char* lstrcpy(char*, const char*);
char* lstrcat(char*, const char*);
int lstrcmp(const char*, const char*);

#endif // LCSTRING_H_INCLUDED
